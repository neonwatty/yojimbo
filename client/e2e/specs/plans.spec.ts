import { test } from '../fixtures/test-fixtures';

/**
 * Plans Panel Tests
 *
 * NOTE: The Plans panel feature is currently hidden in the UI.
 * These tests are skipped until the feature is re-enabled.
 * The Plans components are preserved in the codebase for future restoration.
 */
test.describe('Plans Panel', () => {
  test.skip('plans feature is currently hidden', async () => {
    // This test suite is skipped because the Plans panel feature
    // has been temporarily hidden from the UI.
    // The feature can be restored by uncommenting the Plans button
    // in InstancesPage.tsx and CommandPalette.tsx
  });
});
