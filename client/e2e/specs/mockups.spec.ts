import { test } from '../fixtures/test-fixtures';

/**
 * Mockups Panel Tests
 *
 * NOTE: The Mockups panel feature is currently hidden in the UI.
 * These tests are skipped until the feature is re-enabled.
 * The Mockups components are preserved in the codebase for future restoration.
 */
test.describe('Mockups Panel', () => {
  test.skip('mockups feature is currently hidden', async () => {
    // This test suite is skipped because the Mockups panel feature
    // has been temporarily hidden from the UI.
    // The feature can be restored by uncommenting the Mockups button
    // in InstancesPage.tsx and CommandPalette.tsx
  });
});
